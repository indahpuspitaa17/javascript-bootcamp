// 1 naming convention:
// use camelCased naming (e.g. highScore instead of highscore)

// 2 rules:
// 1) name must contain letters, numbers or symbols $ _
// 2) first character of name can't be a number
// var firstName = prompt('Enter your first name');
// alert(username);

// to log out to the console, use this -> console.log(variableName); 