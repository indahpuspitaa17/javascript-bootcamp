// Initialise an array first. 

const someArr = []

// push() usage
someArr.push('some string')
someArr.push('some other string')
someArr.push('a third other string')

// pop() usage
someArr.pop()

// get the length of an array
someArr.length

// fetch the second last iem in the array
someArr[length - 2];