// Go to index.html and find out what type of HTML element the text 'Coding challenge' is. 
// Is it div, p, h3 or something else? 
// When you know, select it with
document.querySelector();
// querySelector() accepts the name of the element as argument

// To create an element, use
document.createElement()
// createElement() accepts the name of the element as argument

// To add the new h2 under existing one, you need to: 
// - select 'body'
// - append new element to it.